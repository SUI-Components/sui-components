{
  "name": "@schibstedspain/sui-collapsible-readmore",
  "version": "1.1.0",
  "description": "Collapsible read more component for truncating long content and adding an ellipsis.",
  "main": "lib/index.js",
  "scripts": {
    "build": "rm -Rf ./lib && mkdir -p ./lib && npm run build:js && npm run build:styles",
    "build:js": "../../../node_modules/.bin/babel --presets sui ./src --out-dir ./lib",
    "build:styles": "../../../node_modules/.bin/cpx './src/**/*.scss' ./lib"
  },
  "dependencies": {
    "@s-ui/component-dependencies": "1",
    "@schibstedspain/sui-svgiconset": "1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT"
}
