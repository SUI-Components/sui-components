{
  "name": "@schibstedspain/sui-list-image",
  "version": "1.0.0",
  "description": "Displays a list of sui-image-lazy-load, with a customisable limit and onClick callbacks.",
  "main": "lib/index.js",
  "scripts": {
    "build": "rm -Rf ./lib && mkdir -p ./lib && npm run build:js && npm run build:styles",
    "build:js": "../../../node_modules/.bin/babel --presets sui ./src --out-dir ./lib",
    "build:styles": "../../../node_modules/.bin/cpx './src/**/*.scss' ./lib"
  },
  "dependencies": {
    "@schibstedspain/sui-component-dependencies": "latest",
    "@schibstedspain/sui-image-lazy-load": "1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT"
}
