import PrimitiveLoadingIcon from 'components/primitive/loadingIcon/src'

import {H1, Paragraph} from '@s-ui/documentation-library'
export default () => (
  <div className="sui-StudioPreview">
    <H1>PrimitiveLoadingIcon</H1>
    <Paragraph>
      An animated loop used for giving users feedback while the content of a page or section is being loaded.
    </Paragraph>
    <PrimitiveLoadingIcon />
  </div>
)
